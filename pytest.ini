[pytest]
testpaths = tests
markers =
    slow: marks tests that load the ~95 MB nflverse PBP corpus (run in CI nightly only, not on every push)
    cdp: marks browser-level tests that require an isolated Chromium CDP process
